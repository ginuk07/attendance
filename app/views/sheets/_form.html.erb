<%= form_for(@sheet) do |attendance_form| %>
  <% if @sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sheet.errors.count, "error") %> prohibited this Attendance Sheet from being saved:</h2>

      <ul>
      <% @sheet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= attendance_form.label :date %><br />
    <%= attendance_form.date_select :date %>
  </div>
  <h2>Clients</h2>
  <%= render :partial => "clients/client",
             :locals  => { :form => attendance_form } %>
  <div class="actions">
    <%= attendance_form.submit %>
  </div>
<% end %>
