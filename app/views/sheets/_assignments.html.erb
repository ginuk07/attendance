<%= form.fields_for :assignments do |assignment_form| %>
  <% if :sheet_id == @sheet.id %>
    <span class="field">
      <%= assignment_form.select "status", ["Present", "Tardy", "Unexcused Absence", "Excused Absence", "School Closed"], { :include_blank => true } %>
    </span>
  <% end %>
<% end %>
